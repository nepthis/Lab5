package lab5.simulator;

import java.util.Observer;

/**
 * A very bare bones class since it really doesn't have to do anything. Its extender
 * should implement the update from the Observer class which will be called on by the 
 * Observable state.
 * */
public abstract class SimView implements Observer {
	public SimView() {
		
	}
}
